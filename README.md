# مشروع حل ثغرة XSS - المحاضرة 5 🛡️

### شو المشروع؟
هاد تطبيق بسيط (سجل تعليقات مع دخول مستخدمين) بلغة **PHP**، الهدف منه نطبق اللي تعلمناه بالمحاضرة الخامسة عن ثغرات الـ **Cross-Site Scripting**.

### وين كانت المشكلة؟ ⚠️
المشكلة كانت إن الموقع بيعرض تعليقات الناس زي ما هي (Direct Echo). لو "هكر" كتب كود سكريبت خبيث، المتصفح رح يشغله فوراً (Stored XSS)، وهاد بخليه يسرق بيانات أو يخرب الصفحة.



### كيف حليناها؟ ✅
استخدمنا مبدأ الـ **Output Encoding** عن طريق دالة `htmlspecialchars()` في PHP.
* **شو بتعمل؟** بتحول الرموز زي (`<` و `>`) لنصوص عادية.
* **النتيجة:** المتصفح ببطّل يفهم الكود كأمر تشغيل، وبعرضه كأنه نص عادي، وهيك بنكون حمينا الموقع.

### كيف تستخدمه؟
1. شغل ملف الـ `database.sql` عندك في PHPMyAdmin.
2. فوت بحساب: **admin** وباسوورد: **123**.
3. جرب أكتب `<script>alert('XSS')</script>` وشوف كيف الموقع رح يطبعها نص عادي بدون ما يطلع التنبيه (هاد دليل إن الحل شغال).

---
**تم إعداده لتسليم تكليف المحاضرة الخامسة.**
