# مشروع تطبيق عملي على ثغرات Cross-Site Scripting (XSS) 🛡️

## 📝 وصف المشروع
هذا المشروع هو تطبيق عملي لمحتوى **المحاضرة الخامسة**، يهدف إلى محاكاة نظام تسجيل دخول وسجل تعليقات بسيط باستخدام لغة **PHP** وقاعدة بيانات **MySQL**. يركز المشروع بشكل أساسي على كيفية اكتشاف ثغرة **Stored XSS** ومعالجتها برمجياً.

## 🛠️ التقنيات المستخدمة
* **لغة البرمجة:** PHP
* **قاعدة البيانات:** MySQL / MariaDB
* **التنسيق:** HTML5 & CSS3
* **الخادم المحلي:** XAMPP / WampServer

## ⚠️ المشكلة (The Vulnerability)
في النسخ غير المحمية، كان التطبيق يسمح للمستخدمين بإدخال نصوص تحتوي على وسوم برمجية مثل `<script>`. وبسبب عرض هذه النصوص مباشرة باستخدام أمر `echo` دون معالجة، يقوم المتصفح بتنفيذ هذه السكربتات، مما قد يؤدي إلى:
1. سرقة كوكيز الجلسة (Session Cookies).
2. إعادة توجيه المستخدم لمواقع مشبوهة.
3. تشويه واجهة الموقع.



## ✅ الحل التطبيقي (The Solution)
بناءً على ما تم دراسته، تم تطبيق استراتيجية **Output Encoding** لحماية الموقع. 

تم استخدام دالة `htmlspecialchars()` في جميع مواضع عرض بيانات المستخدم (اسم المستخدم والتعليقات). 

### الكود قبل المعالجة (Vulnerable):
```php
echo "<div>" . $row
